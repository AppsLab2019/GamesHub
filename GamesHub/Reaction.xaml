<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="GamesHub.Reaction">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Text="Player1" 
                    x:Name="Player1"
                    Clicked="Player1Score"
                    Grid.Row="0" />
            <Button Text="Player2"
                    Clicked="Player2Score"
                    x:Name="Player2"
                    Grid.Row="2"/>
            <Button Text="START"
                    x:Name="MainButton"
                    Clicked="StartButton"
                    BackgroundColor="White"
                    Grid.Row="1"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>